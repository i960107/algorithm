def solution(distance: int, rocks: list, n: int) -> int:
    answer = 0
    # 각 바위 사이 거리의 최솟값
    left = 0
    # 각 바위 사이 거리의 최댓값
    right = distance
    while left < right:
        if i
    return answer


print(solution(25, [2, 15, 11, 21, 17], 2))
